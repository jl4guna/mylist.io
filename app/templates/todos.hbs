<ul class="collection with-header">
    <li class="collection-header">
        <h4>ToDos</h4>
    </li>

    {{#each model as |todo|}}
        <li class="collection-item row {{if todo.isEditing "large"}} {{if todo.isCompleted "completed"}} {{if todo.hasDirtyAttributes "unsaved"}}">
            <div class="col s1">
                {{epic-checkbox value=todo.isCompleted}}
            </div>
            <div class="col s7">
                {{inliner- value=todo.title isEditing=todo.isEditing isCompleted=todo.isCompleted}}
            </div>
            <div class="col s4">
                {{controls- todo=todo save=saveItem delete=deleteItem restore=restoreItem}}
            </div>
        </li>
    {{else}}
        <li class="collection-item">
            <span>Uh... No items added yet :(</span>
        </li>
    {{/each}}
</ul>

<button class="fa fa-floppy-o waves-effect waves-light btn {{if areDirtyItems "disabled"}}" disabled="{{if areDirtyItems true null}}" {{action "saveAll"}}>
    Save All
</button>

<button class="fa fa-undo waves-effect waves-light btn {{if areDirtyItems "disabled"}}" disabled="{{if areDirtyItems true null}}" {{action "restoreAll"}}>
    Restore All
</button>

<div class="fixed-action-btn" style="bottom: 45px; right: 24px;">
    <a class="btn-floating btn-large waves-effect waves-light red" {{action "add"}}><i class="fa fa-plus"></i></a>
</div>
