<div class="secondary-content">
    <button class="fa fa-floppy-o waves-effect waves-light btn-flat {{if areSaveAndRestoreDisabled "disabled"}}" disabled="{{if areSaveAndRestoreDisabled true null}}" {{action "save"}}>
    </button>

    <button class="fa fa-undo waves-effect waves-light btn-flat {{if areSaveAndRestoreDisabled "disabled"}}" disabled="{{if areSaveAndRestoreDisabled true null}}" {{action "restore"}}>
    </button>

    <button class="fa fa-trash waves-effect waves-light btn-flat" {{action "delete"}}>
    </button>
</div>
